install.packages("updateR")
install.packages("installr")
installr::updateR()
library(reshape)
install.packages("dplyr")
install.packages("reshape")
install.packages("ez")
install.packages("lrd")
install.packages("vectsects")
install.packages("Vectsects")
install.packages("VectSects")
install.packages("psych")
install.packages("Hmisc")
install.packages("PsychReport")
install.packages("psychReport")
install.packages("caret")
2406*.10
setwd("C:/Users/nickm/OneDrive/Documents/GitHub/DRM-Reactivity/4 Analysis/Free Recall/Ex 2A")
####Set up####
##read in data
dat = read.csv("Data/ex2a.csv")
##load libraries
library(reshape)
library(ez)
options(scipen = 999)
View(dat)
##fix column names
colnames(dat)[2:3] = c("score", "group")
dat = dat[ , -c(4:6)]
dat = dat[ , c(1, 3:5, 2)]
##inspect the data
table(dat$group)
##inspect the data
table(dat$group) / 2
##subset out by memory type
correct = subset(dat,
dat$mem_type == 'true')
drm = subst(dat,
dat$mem_type == "DRM")
drm = subset(dat,
dat$mem_type == "DRM")
####Set up####
##read in data
dat = read.csv("Data/ex2a.csv")
##load libraries
library(reshape)
library(ez)
options(scipen = 999)
##fix column names, drop unused columns, and reorder
colnames(dat)[2:3] = c("score", "group")
dat = dat[ , -c(4:6)]
dat = dat[ , c(1, 3, 5, 2)]
####Set up####
##read in data
dat = read.csv("Data/ex2a.csv")
##load libraries
library(reshape)
library(ez)
options(scipen = 999)
##fix column names, drop unused columns, and reorder
colnames(dat)[2:3] = c("score", "group")
dat = dat[ , -c(4:6)]
dat = dat[ , c(1, 3, 5, 2)]
##how many participants?
table(dat$group) / 2 #32 global, 33 item, 31 read (might need to get these numbers up a bit)
##subset out by memory type
correct = subset(dat,
dat$mem_type == 'true')
drm = subset(dat,
dat$mem_type == "DRM")
##get data patterns
tapply(correct$score, correct$group, mean, na.rm = T)
tapply(drm$score, drm$group, mean, na.rm = T)
View(dat)
####Screen participants####
item = subset(dat,
dat$group == "Item")
####Screen participants####
item = subset(correct,
correct$group == "Item")
View(item)
item2 = subset(correct,
correct$group == "Item")
View(item2)
item2 = subset(drm,
drm$group == "Item")
View(item2)
dat$score[dat$score > 1] = 1
##how many participants?
table(dat$group) / 2 #32 global, 33 item, 31 read (might need to get these numbers up a bit)
##subset out by memory type
correct = subset(dat,
dat$mem_type == 'true')
drm = subset(dat,
dat$mem_type == "DRM")
##get data patterns
tapply(correct$score, correct$group, mean, na.rm = T)
tapply(drm$score, drm$group, mean, na.rm = T)
####Screen participants####
item = subset(correct,
correct$group == "Item")
item2 = subset(drm,
drm$group == "Item")
View(item2)
read = subset(correct,
correct$group == "Read")
read2 = subset(drm,
drm$group == "Read")
View(read)
View(read2)
View(read)
glob = subset(correct,
correct$group == "Global")
glob = subset(correct,
correct$group == "Global")
glob2 = subset(drm,
drm$group == "Global")
View(glob)
View(glob2)
####ANOVA####
ezANOVA(dat,
dv = score,
wid = Sub.ID,
between = group,
within = mem_type,
type = 3,
detailed = T)
##subset out by memory type
correct = subset(dat,
dat$mem_type == 'true')
##get data patterns
tapply(correct$score, correct$group, mean, na.rm = T)
tapply(drm$score, drm$group, mean, na.rm = T)
####Set up####
##read in data
dat = read.csv("Data/ex2a.csv")
##load libraries
library(reshape)
library(ez)
options(scipen = 999)
##fix column names, drop unused columns, and reorder
colnames(dat)[2:3] = c("score", "group")
dat = dat[ , -c(4:6)]
dat = dat[ , c(1, 3, 5, 2)]
dat$score[dat$score > 1] = 1
##how many participants?
table(dat$group) / 2 #32 global, 33 item, 31 read (might need to get these numbers up a bit)
##subset out by memory type
correct = subset(dat,
dat$mem_type == 'true')
drm = subset(dat,
dat$mem_type == "DRM")
##get data patterns
tapply(correct$score, correct$group, mean, na.rm = T)
tapply(drm$score, drm$group, mean, na.rm = T)
####Screen participants####
item = subset(correct,
correct$group == "Item")
item2 = subset(drm,
drm$group == "Item")
read = subset(correct,
correct$group == "Read")
read2 = subset(drm,
drm$group == "Read")
glob = subset(correct,
correct$group == "Global")
glob2 = subset(drm,
drm$group == "Global")
####ANOVA####
ezANOVA(dat,
dv = score,
wid = Sub.ID,
between = group,
within = mem_type,
type = 3,
detailed = T)
####Set up####
##read in data
dat = read.csv("Data/ex2a.csv")
##load libraries
library(reshape)
library(ez)
options(scipen = 999)
##fix column names, drop unused columns, and reorder
colnames(dat)[2:3] = c("score", "group")
dat = dat[ , -c(4:6)]
dat = dat[ , c(1, 3, 5, 2)]
dat$score[dat$score > 1] = 1
##how many participants?
table(dat$group) / 2 #34 global, 37 item, 32 read (might need to get these numbers up a bit)
##subset out by memory type
correct = subset(dat,
dat$mem_type == 'true')
drm = subset(dat,
dat$mem_type == "DRM")
##get data patterns
tapply(correct$score, correct$group, mean, na.rm = T)
tapply(drm$score, drm$group, mean, na.rm = T)
####ANOVA####
ezANOVA(dat,
dv = score,
wid = Sub.ID,
between = group,
within = mem_type,
type = 3,
detailed = T)
##get data patterns
tapply(correct$score, correct$group, mean, na.rm = T)
##how many participants?
table(dat$group) / 2 #34 global, 37 item, 32 read (might need to get these numbers up a bit)
##data screening
item = subset(dat,
dat$group == "Item")
View(item)
####Set up####
##read in data
dat = read.csv("Data/ex2a.csv")
##load libraries
library(reshape)
library(ez)
options(scipen = 999)
##fix column names, drop unused columns, and reorder
colnames(dat)[2:3] = c("score", "group")
dat = dat[ , -c(4:6)]
dat = dat[ , c(1, 3, 5, 2)]
dat$score[dat$score > 1] = 1
##how many participants?
table(dat$group) / 2 #34 global, 37 item, 32 read (might need to get these numbers up a bit)
##data screening
item = subset(dat,
dat$group == "Item")
##subset out by memory type
correct = subset(dat,
dat$mem_type == 'true')
View(item)
drm = subset(dat,
dat$mem_type == "DRM")
##get data patterns
tapply(correct$score, correct$group, mean, na.rm = T)
tapply(drm$score, drm$group, mean, na.rm = T)
####ANOVA####
ezANOVA(dat,
dv = score,
wid = Sub.ID,
between = group,
within = mem_type,
type = 3,
detailed = T)
##how many participants?
table(dat$group) / 2 #34 global, 37 item, 32 read (might need to get these numbers up a bit)
tapply(drm$score, drm$group, mean, na.rm = T)
####Set up####
##read in data
dat = read.csv("Data/ex2a.csv")
##load libraries
library(reshape)
library(ez)
options(scipen = 999)
##fix column names, drop unused columns, and reorder
colnames(dat)[2:3] = c("score", "group")
dat = dat[ , -c(4:6)]
dat = dat[ , c(1, 3, 5, 2)]
dat$score[dat$score > 1] = 1
##how many participants?
table(dat$group) / 2 #37 global, 42 item, 40 read (might need to get these numbers up a bit)
##data screening
item = subset(dat,
dat$group == "Item")
##subset out by memory type
correct = subset(dat,
dat$mem_type == 'true')
drm = subset(dat,
dat$mem_type == "DRM")
##get data patterns
tapply(correct$score, correct$group, mean, na.rm = T)
tapply(drm$score, drm$group, mean, na.rm = T)
####ANOVA####
ezANOVA(dat,
dv = score,
wid = Sub.ID,
between = group,
within = mem_type,
type = 3,
detailed = T)
